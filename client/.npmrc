# Mirror the ietf-tools/red supply-chain posture: pin exact versions.
save-exact=true
# red also sets min-release-age and allow-git=none in its toolchain; npm
# ignores unknown keys, so they are documented here rather than relied upon.
