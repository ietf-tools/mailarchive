{
  "name": "mailarchive-message-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for email message detail pages",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --env staging",
    "dev": "wrangler dev --env staging",
    "dev-remote": "wrangler dev --env staging --remote"
  },
  "dependencies": {
    "mustache": "4.2.0"
  },
  "devDependencies": {
    "wrangler": "4.81.1"
  }
}
